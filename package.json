{
  "name": "node-blog-api",
  "version": "1.0.0",
  "description": "An example blog api written in plain node which uses a file as a data store",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kyle Willett",
  "license": "ISC",
  "dependencies": {
    "sanitize-html": "2.17.0"
  }
}
